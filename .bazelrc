# Default C++ standard
build --cxxopt=/std:c++20

# Debug configuration
build:debug --compilation_mode=dbg
build:debug --cxxopt=/std:c++20

# Release configuration  
build:release --compilation_mode=opt
build:release --cxxopt=/std:c++20